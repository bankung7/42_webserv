#include "Server.hpp"

int main(int argc, char **argv) {

    // check argument and the incoming config file
    (void)argc, (void)argv;

    // TODO : Server part
    Server();

    return (0);
}